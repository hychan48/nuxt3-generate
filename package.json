{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "generate_preview":"nuxt generate && nuxt preview",
    "generate_preview_rm":"cmd rmdir /Q /S '.nuxt' ; nuxt generate && nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "3.0.0"
  }
}
